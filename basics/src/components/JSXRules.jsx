import React from "react";

const JSXRules = () => {
  return (
    <div>
      <h1>JSX Rules</h1>
      <p>JSX must return a single parent element</p>
      <p>JSX must use a closing tag</p>
      <p>
        Jsx attributes need to be camelCase (e.g. classname instead of class).
      </p>
    </div>
  );
};

export default JSXRules;
